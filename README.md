
  # Create HTML Use Case Page

  This is a code bundle for Create HTML Use Case Page. The original project is available at https://www.figma.com/design/2F8yeJmq3y6ebaA6qadFKF/Create-HTML-Use-Case-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  